server.port=5000
ml.service.url=http://localhost:8000
spring.application.name=dispatch-backend
spring.main.banner-mode=console
logging.level.com.fleet.dispatch=INFO

# MongoDB Configuration (reads MONGODB_URI env var; falls back to local instance)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/quantum_express}
spring.data.mongodb.database=quantum_express
